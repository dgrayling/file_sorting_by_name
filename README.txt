Plan:

1. Collect a mapping of file names to directories
2. Create a mapping of common substrings of the file names to file names
3. Present that data to the console
4. Write code to convert the mappings of file names to a directory structure